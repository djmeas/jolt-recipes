services:
  web:
    build: .
    ports:
      - "8385:3000"
    volumes:
      - ./.data:/app/.data
    env_file:
      - .env
    environment:
      - NUXT_SITE_PASSWORD=${SITE_PASSWORD}
      - NUXT_PUBLIC_SITE_NAME=${SITE_NAME:-Jolt Recipes}
    restart: unless-stopped